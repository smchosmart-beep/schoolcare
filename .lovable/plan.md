

# F1~F8 버튼 2줄 배치 + 설정/더보기 재배치

## 변경 사항

### `src/components/admin/VisitRecordModal.tsx` — 108~168줄 레이아웃 수정

현재 한 줄 `flex-wrap`으로 되어 있는 구조를 아래와 같이 변경:

```text
┌─────────────────────────────────────────────────┐
│ [F1] [F2] [F3] [F4]     [+더보기 ▼] [⚙️]       │
│ [F5] [F6] [F7] [F8]                             │
└─────────────────────────────────────────────────┘
```

- **1줄**: F1~F4 버튼 + 오른쪽에 `+더보기` 드롭다운과 ⚙️ 설정 버튼
- **2줄**: F5~F8 버튼

구체적으로:
- 바깥 `div`를 `space-y-1`로 변경 (2줄 구분)
- 1번째 줄: `flex items-center gap-1.5` → F1~F4 버튼 + `ml-auto`로 더보기/설정 버튼 오른쪽 정렬
- 2번째 줄: `flex items-center gap-1.5` → F5~F8 버튼만 배치

코드 변경은 108~168줄 영역만 해당하며, 나머지 로직은 그대로 유지합니다.

